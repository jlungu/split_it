VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Set to your Railway API URL when web and API are deployed separately (Vercel + Railway).
# Leave empty/omit when using Railway-only deployment (API serves web from same domain).
VITE_API_URL=https://your-api.railway.app
